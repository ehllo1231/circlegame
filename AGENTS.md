# AGENTS.md

본 문서는 이 프로젝트에서 작업하는 에이전트(및 협업자)가 따라야 할 규칙과
코드베이스 구조, 변경 원칙을 정의합니다. 가능한 한 간결하고 모듈화된 변경을
선호하며, 기존 동작(게임 플레이 감각, 입력 매핑, 스테이지 전이 로직 등)의
의도치 않은 변형을 방지합니다.

---

## 1) 프로젝트 개요 / 실행

- **타입**: 순수 HTML + JS 캔버스 게임 (번들링/빌드 불필요)
- **실행 방법(택1)**  
  - 로컬 파일로 `index.html` 열기  
  - 혹은 간단 서버로 실행:  
    - Python: `python -m http.server 5173` 후 `http://localhost:5173` 접속  
    - Node 패키지(있는 경우): `npx http-server .`
- **프로그램적 체크(자동 실행 명령)**: 없음  
  - 테스트/린트 설정이 없다면 **외부 명령 실행을 시도하지 않습니다.**
  - 커밋 전 수동 체크리스트로 품질을 보증합니다(하단 참조).

---

## 2) 코드 구조(요약)

### 전체 흐름
- 시작 페이지에서 UI 오버레이와 캔버스를 준비한 뒤 설정값에 맞춰 캔버스 크기를 지정하고 `Game` 인스턴스를 생성합니다  
  *(index.html:28, index.html:53, index.html:65)*.
- `Game` 클래스는 캔버스와 중심 좌표를 계산하고 주요 서브 시스템(UI, 입력, 스테이지, 장면)을 초기화한 뒤 이벤트를 묶어 전체 게임 루프를 관리합니다  
  *(Game.js:16, Game.js:35, Game.js:42, Game.js:96)*.

### 핵심 컨트롤러
- `Game`은 애니메이션 루프에서 스테이지 진행/점수 갱신, 프롤로그·배경 효과·UI 표시 여부를 결정하고, `GameScene`에 프레임 업데이트/렌더링을 위임합니다  
  *(Game.js:237, Game.js:273, Game.js:321, Game.js:345)*.
- 화면 상 상호작용(스테이지 선택·재시작·하이스코어 리셋 등)은 `UIController`가 DOM 이벤트로 처리하며, 점수 텍스트 중앙 부유 표시도 담당합니다  
  *(UIController.js:16, UIController.js:80)*.
- 키 입력은 `InputController`가 Space/F6/F7 등 매핑을 읽어 `Game`의 콜백을 호출하는 방식으로 처리합니다  
  *(InputController.js:16, InputController.js:32)*.

### 게임 장면
- `GameScene`은 플레이어, 장애물 관리자, 리듬/눈 효과를 묶어 매 프레임 이동·충돌·스폰 로직을 수행하고 그립니다  
  *(GameScene.js:7, GameScene.js:91, GameScene.js:163)*.
- 플레이어는 궤도를 따라 회전하며 방향 전환과 장애물 근접 체크만 수행합니다  
  *(Player.js:4, Player.js:28, Player.js:41)*.
- `ObstacleManager`는 스폰 간격/확률, 속도 가중치, 충돌 시 파편 이펙트를 한곳에서 처리하며 **점수에 따라 스폰 템포를 가속**합니다  
  *(ObstacleManager.js:5, ObstacleManager.js:60, ObstacleManager.js:136, ObstacleManager.js:177)*.
- 실제 스파이크 도형은 `Obstacle`이 **회전 좌표계**로 삼각형을 그려 표현합니다  
  *(Obstacle.js:2, Obstacle.js:18)*.

### 스테이지 시스템
- `StageOrchestrator`는 선택된 스테이지부터 순서를 결정하고, 진행 중 스테이지의 **상태·배경·스폰 가능 여부**를 외부에 제공합니다  
  *(StageOrchestrator.js:1, StageOrchestrator.js:72, StageOrchestrator.js:118)*.
- 각 스테이지는 `StageManager`를 상속해 여러 `StagePhase`를 순차 실행하며 `Config` 값을 **실시간 변경**합니다. 페이즈 경계 진입/이탈 시 스폰·눈 효과를 토글하고 **페이드 아웃/점수 숨김 시간**을 제어합니다  
  *(StageManager.js:33, StageManager.js:65, StageManager.js:118, StageManager.js:152)*.
- `Stage1`, `Stage2`는 페이즈별로 스폰 간격/멀티 스폰 확률/눈보라 속성 등을 달리해 난이도를 상승시킵니다  
  *(Stage1.js:4, Stage1.js:70; Stage2.js:1, Stage2.js:180)*.
- `Stage2`는 **프롤로그 연출**을 추가로 제공하며, 특수 스파이크·먼지 이펙트/배경 떨림/충돌 무적 시간을 제어합니다  
  *(Stage2.js:6, Stage2.js:33, Stage2.js:95, Stage2.js:160)*.

### 엔티티와 효과
- **리듬 파동**: 일정 프레임마다 궤도와 플레이어를 확대/축소해 시각적 박자를 부여합니다  
  *(RhythmEffect.js:3, RhythmEffect.js:12, RhythmEffect.js:22)*.
- **눈발**: 바람/중력 설정을 스냅샷으로 보관해 안정적인 파티클 이동을 구현합니다  
  *(SnowEffect.js:3, SnowEffect.js:16, SnowEffect.js:70)*.
- **전역 설정**: `Config`에서 관리되며, 디버그 패널/스테이지 전환으로 수정된 값을 `ConfigDefaults`로 쉽게 초기화할 수 있습니다  
  *(Config.js:2, ConfigDefaults.js:17)*.

### 지원 도구
- `StageThemeManager`, `StageBackgroundFader`는 스테이지별 색상 테마와 페이드 애니메이션을 DOM 요소에 적용해 전환 연출을 담당합니다  
  *(StageThemeManager.js:1, StageBackgroundFader.js:3)*.
- `DebugController`(F6)는 스폰/눈/장애물/플레이어 파라미터를 **실시간 조정**할 수 있는 오버레이를 띄워 실험적 튜닝을 돕습니다  
  *(Game.js:64, DebugController.js:1)*.

---

## 3) 설계 원칙(코드 작성 규칙)

- **SOLID 원칙 최대 준수**
  - 단일 책임, 개방/폐쇄, 리스코프 치환, 인터페이스 분리, 의존 역전.
- **SRP 기반 모듈화**
  - 컨트롤러/장면/엔티티/효과/스테이지를 분리하고, 공용 유틸/설정은 `Config`, `ConfigDefaults`를 경유.
- **확장성·유지보수성 최우선**
  - 새 기능은 **기존 인터페이스를 보존**하고, 교차 영향(성능/입력/연출)을 최소화하는 **적응형 확장**으로 구현.

**변경 기본 규칙**
- 퍼블릭 메서드 시그니처와 이벤트 훅(입력 콜백, 씬 업데이트/렌더 흐름)은 **호환성 유지**.
- 프레임 루프(업데이트→렌더) 순서와 시간기반 로직(delta/타이밍)은 **변경 금지**.
- `Config` 변경은 **스테이지/디버그 경로**를 통해 수행(직접 전역 하드코딩 금지).
- 외부 라이브러리 추가는 원칙적으로 금지(필요 시 명확한 근거와 최소 도입).

---

## 4) 코딩 컨벤션

- **파일/모듈**
  - 역할 단위로 파일 분리(Controller/Scene/Entity/Effect/Stage).
  - 공용 상수/설정은 `Config*` 계열로 관리.
- **명명**
  - 클래스: PascalCase (`GameScene`, `ObstacleManager`)  
  - 인스턴스/함수: camelCase (`updateFrame`, `spawnObstacle`)
- **스타일**
  - 가독성 우선(짧은 함수, 조기 반환, 사이드이펙트 축소).
  - 매직 넘버는 상수화, 상수는 상단에 모아 정의.
- **주석**
  - 파일 헤더: 역할/입력/출력/부작용 한 줄 요약.
  - 복잡한 수학/좌표 변환은 식 대신 **개념 설명을 우선**(필요 시 간단 예시 추가).

---

## 5) 변경 가이드(예시)

- 입력 확장(예: 키 매핑 추가)
  - `InputController`에만 로직을 추가하고, `Game`에는 **콜백 계약**으로 연결.
- 장애물 패턴 추가
  - `ObstacleManager`에 스폰 전략 클래스를 **플러그인 형태**로 주입.
- 스테이지 효과 추가
  - `StageManager` 페이즈 내 `Config` 트윅으로 구현하고, 전역값 직접 수정 금지.

---

## 6) 수동 QA 체크리스트(커밋 전)

1. **기동**: `index.html` 실행 시 UI 오버레이, 캔버스 크기, 초기 `Game` 생성 정상.  
2. **입력**: Space/F6/F7 등 기본 매핑 정상 동작(정지/디버그/스테이지).  
3. **루프**: 프레임 드랍/렉 없이 업데이트→렌더 순서 유지.  
4. **장면**: 플레이어 궤도 회전, 방향 전환, 충돌 판정 정상.  
5. **장애물**: 점수 증가에 따른 스폰 템포 가속 확인, 파편 이펙트 정상.  
6. **스테이지**: 페이즈 전환 시 스폰/눈 토글, 페이드/점수 숨김 시간 정상.  
7. **연출**: Stage2 프롤로그(특수 스파이크/먼지, 배경 떨림, 무적 시간) 정상.  
8. **디버그**: F6 오버레이로 주요 파라미터 조정 시 실시간 반영.  
9. **리셋**: 스테이지 재시작/하이스코어 리셋 시 상태 초기화 문제 없음.

---

## 7) 커밋 원칙(선택)

- 한 커밋 = 한 책임(기능 추가 / 리팩터 / 버그픽스 분리).
- 메시지 프리픽스 예: `[core]`, `[scene]`, `[stage]`, `[effect]`, `[ui]`.
- 리팩터링은 기능 변경 없이 진행하고, 필요 시 별도 커밋으로 분리.

---

## 8) 경계(Do / Don’t)

- **Do**
  - 로컬 상태를 명확히 하고, 공용 상태 접근은 컨트롤러/관리자를 통해 간접화.
  - 좌표/시간 관련 수학은 유닛별로 캡슐화하고 재사용.
- **Don’t**
  - 프레임 루프 흐름 변경, 전역 설정의 직접 수정보다 `Config` 경유.
  - 외부 네트워크/스토리지 접근 추가, 대형 라이브러리 도입.

---

## 9) 문의/결정

- 설계 충돌이나 스테이지 체계 변경이 필요한 경우, 먼저 문서(본 파일) 상단에
  **변경 제안 요약**을 추가한 뒤 구현합니다.